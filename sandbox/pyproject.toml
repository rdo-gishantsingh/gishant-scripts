[project]
name = "sandbox"
version = "0.1.0"
description = "Sandbox project generation and NAS cleanup (DESTRUCTIVE -- use with care)"
requires-python = ">=3.11"
dependencies = ["typer>=0.9.0", "rich>=13.0.0", "python-dotenv>=1.0.0"]

[project.scripts]
sandbox = "sandbox.cli:app"

[tool.ruff]
line-length = 120
target-version = "py311"
